include ../../assets/pug/_head
include ../../assets/pug/_scripts

- title = 'Escribo un texto'
- subtitle = 'Escribimos nuestros textos / Revisamos nuestros textos / Compartimos nuestros textos'

<!DOCTYPE html>
html(lang="es")
    +head
        style.
            /*
            .ex1 {}
            */
    body
        #app.container.my-5.animate__animated.animate__fadeIn.animate__delay-1s
            h1 #{title}
            if subtitle.length>1
                h2 #{subtitle}

            .instruccion.mt-5 Lee el siguiente texto sobre la diferencia entre vivir con el servicio de agua potable y sin él.
            
            .row: .col-12: img(src="aimg/a1.png").w-100
            
            .instruccion.mt-5 Marca así <checkmark></checkmark> las características que tiene el texto anterior.

            .row.mt-3.ex1
                .col-md-6(v-for="(i, index) in tempsobj.a")
                    check(v-model="r[index]" :ref="refCount()"  @isright="right++" :answer="true" :text="i")

            





            finalize(@evaluate="finalizar" @reset="reset" :resultado="resultado" :right="right" :total="total")
        +scripts
            script.
                app.$set(app.tempsobj, 'a', [
                    'Inicia con mayúscula. Tiene punto final.',
                    'Tiene mayúsculas después de cada punto.',
                    'Las oraciones están separadas por puntos.',
                    'Las palabras están bien escritas.',
                    'Tiene título.',
                    'Tiene inicio, desarrollo y cierre.'
                ])