include ../../assets/pug/_head
include ../../assets/pug/_scripts

- title = 'Practico parafrasear'
- subtitle = 'Los datos, los argumentos y las opiniones'

<!DOCTYPE html>
html(lang="es")
    +head
        style.
            .inputbox {
                margin-bottom: 20px;
                background: #FDE8CF;
                padding: 10px;
                border-radius: 10px;
                justify-content: flex-start;
            }
            .inputbox input {
                width: 60px;
                padding: 2px;
                font-size: 1.6rem;
                margin-right: 8px;
            }

    body
        #app.container.my-5.animate__animated.animate__fadeIn.animate__delay-1s
            h1 #{title}
            if subtitle.length>1
                h2 #{subtitle}

            .instruccion.mt-5 Escribe #[strong A] si la oración es un argumento, #[strong D] si es un dato u #[strong O] si es una opinión.

            .row.mt-3.ex1
                .col
                    inputbox(v-model="r[0]" :ref="refCount()" @isright="right++" answer="O" type="text" placeh="-")
                        template(v-slot:after=""): .t Pienso que la migración en México podría eventualmente afectar el crecimiento de población.
                    inputbox(v-model="r[1]" :ref="refCount()" @isright="right++" answer="A" type="text" placeh="-")
                        template(v-slot:after=""): .t El nivel alto de migración no afecta el crecimiento de población en México.
                    inputbox(v-model="r[2]" :ref="refCount()" @isright="right++" answer="D" type="text" placeh="-")
                        template(v-slot:after=""): .t El crecimiento de población en nuestro país se ha mantenido con una constante de 1 % anual desde el año 2014.
            





            finalize(@evaluate="finalizar" @reset="reset" :resultado="resultado" :right="right" :total="total")
        +scripts
            script.
                //app.$set(app.tempsobj, 'a', [])