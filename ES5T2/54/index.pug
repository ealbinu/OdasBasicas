include ../../assets/pug/_head
include ../../assets/pug/_scripts

- title = 'Aprendo características de poemas'
- subtitle = 'La rima en los poemas'

<!DOCTYPE html>
html(lang="es")
    +head
        style.
            .items1,
            .items2 {
                display: flex;
                flex-direction: column;
                justify-content: space-around;
                min-height: 100%;
            }
            .item {
                text-align: center;
                background: #B96E9F;
                color: #fff;
                margin-bottom: 5px;
                border-radius: 10px;
            }
            .item .numbers {
                color: #fff;
                font-size: 2.4rem;
            }
            .inputbox {
                justify-content: flex-start;
                background: #4CBAC0;
                color: #fff;
                padding: 10px;
                border-radius: 10px;
                margin-bottom: 5px;
            }
            .inputbox input {
                width: 40px;
                margin-right: 6px;
                font-size: 1.2rem;
                border-radius: 4px;
            }

    body
        #app.container.my-5.animate__animated.animate__fadeIn.animate__delay-1s
            h1 #{title}
            if subtitle.length>1
                h2 #{subtitle}

            .instruccion.mt-5 Relaciona cada recurso literario con el fragmento de poema que le corresponde.

            .row.mt-3.ex1
                .col-5.items1
                    .item
                        numbers A.
                        div Aliteración
                    .item
                        numbers B.
                        div Hipérbole
                    .item
                        numbers C.
                        div Metáfora
                    .item
                        numbers D.
                        div Repetición
                .col-7.items2
                    inputbox(v-model="r[0]" :ref="refCount()" @isright="right++" answer="C" type="text" placeh="-")
                        template(v-slot:after="") Sus perlas que resplandecen a cualquiera al sonreír...
                    inputbox(v-model="r[1]" :ref="refCount()" @isright="right++" answer="B" type="text" placeh="-")
                        template(v-slot:after="") Lloré un mar de lágrimas...
                    inputbox(v-model="r[2]" :ref="refCount()" @isright="right++" answer="D" type="text" placeh="-")
                        template(v-slot:after="") Canta, canta mi corazón de alegría...
                    inputbox(v-model="r[3]" :ref="refCount()" @isright="right++" answer="A" type="text" placeh="-")
                        template(v-slot:after="") Ramo de rosas rojas que rozaron mi rostro...
            





            finalize(@evaluate="finalizar" @reset="reset" :resultado="resultado" :right="right" :total="total")
        +scripts
            script.
                //app.$set(app.tempsobj, 'a', [])