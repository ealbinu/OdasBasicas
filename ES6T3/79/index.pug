include ../../assets/pug/_head
include ../../assets/pug/_scripts
include ../../assets/pug/_crossword

- title = 'Juego con las figuras retóricas'
- subtitle = '¿Recuerdas qué es?'

<!DOCTYPE html>
html(lang="es")
    +head
        style.
            .crowsswordmod{
                background: #FDDC6B;
            }
            .crowsswordmod .box {
                width: 6.666%;
                min-height: 30px;
                outline: none;
            }
            .label {
                font-size: 0.9rem;
                margin-bottom: 10px;
                background: #f5f5f5;
                padding: 6px;
                border-radius: 6px;
            }
            @media (max-width: 680px) {
                .label {
                    font-size: 0.7rem;
                }
            }
            .crowsswordmod .number{
                background: transparent;
                color: var(--sec);
                font-family: 'barnacle_boyregular';

            }

    body
        #app.container.my-5.animate__animated.animate__fadeIn.animate__delay-1s
            h1 #{title}
            if subtitle.length>1
                h2 #{subtitle}

            .instruccion.mt-5 Resuelve el crucigrama con los nombres de las figuras retóricas.

            .row.mt-3.ex1.centerrow
                .col-md-5
                    numbers(smaller c=1) Verticales:
                    labeltext(num="1.") Consiste en la reagrupación de sílabas o palabras para modificar el significado de una oración y ocultar un doble sentido.
                    labeltext(num="2.") Es la representación escrita de un sonido como: clic, crack, plaf, puff, pss, etcétera.
                    labeltext(num="3.") Se da a entender una cosa expresando lo opuesto de lo que, en realidad, se quiere decir o se piensa.
                    labeltext(num="4.") Consiste en la repetición de un mismo sonido o sonidos similares para producir un efecto sonoro en la lectura.

                    numbers(smaller c=4) Horizontales:
                    labeltext(num="5.") Consiste en evitar la repetición innecesaria de palabras para generar mayor fluidez y ritmo.
                    labeltext(num="6.") Consiste en establecer una comparación explícita entre dos términos de cualidades semejantes.
                    labeltext(num="7.") Consiste en expresar un objeto o una situación con un significado distinto al habitual, sin usar palabras de enlace entre los dos significados.
                    labeltext(num="8.") Es la repetición de una palabra o un conjunto de palabras al comienzo de una frase o un verso.
                .col-md
                    - var hor = []
                    - var ver = []
                    //[x,y,code,word, isexample]
                    
                    - hor.push([8, 4, '5', 'ELIPSIS'])
                    - hor.push([3, 7, '6', 'SÍMIL'])
                    - hor.push([3, 9, '7', 'METÁFORA'])
                    - hor.push([1, 12, '8', 'REITERACIÓN'])

                    - ver.push([2, 4, '1', 'CALAMBUR'])
                    - ver.push([6, 3, '2', 'ONOMATOPEYA'])
                    - ver.push([11, 3, '3', 'IRONÍA'])
                    - ver.push([14, 1, '4', 'ALITERACIÓN'])


                    +crosswordmod(0, 15,14, hor, ver)


            finalize(@evaluate="finalizar" @reset="reset" :resultado="resultado" :right="right" :total="total")
        +scripts
            script.
                //app.$set(app.tempsobj, 'a', [])