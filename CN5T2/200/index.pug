include ../../assets/pug/_head
include ../../assets/pug/_scripts

- title = 'Reconozco cómo se mueve el sonido'
- subtitle = ''

<!DOCTYPE html>
html(lang="es")
    +head
        style.
            /*
            .ex1 {}
            */
    body
        #app.container.my-5.animate__animated.animate__fadeIn.animate__delay-1s
            h1 #{title}
            if subtitle.length>1
                h2 #{subtitle}

            .instruccion.mt-5 Observa las ilustraciones y marca así <markex></markex> aquellas que puedan afectar el oído por el exceso de ruido.

            .row.mt-3.ex1
                .col-md-3(v-for="(i, index) in [true, false, true, false, false, true, false, true]")
                    check(v-model="r[index]" :ref="refCount()"  @isright="right++" :answer="i" set-mark="../../assets/aimg/markex.svg")
                        img(:src="'aimg/a'+(index+1)+'.png'").img-fluid
            





            finalize(@evaluate="finalizar" @reset="reset" :resultado="resultado" :right="right" :total="total")
        +scripts
            script.
                //app.$set(app.tempsobj, 'a', [])