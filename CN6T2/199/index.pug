include ../../assets/pug/_head
include ../../assets/pug/_scripts

- title = 'Examino mi huella ecológica'
- subtitle = 'Mejoremos nuestro ambiente'

<!DOCTYPE html>
html(lang="es")
    +head
        style.
            /*
            .ex1 {}
            */
    body
        #app.container.my-5.animate__animated.animate__fadeIn.animate__delay-1s
            h1 #{title}
            if subtitle.length>1
                h2 #{subtitle}

            .instruccion.mt-5 Marca así <checkmark></checkmark> las acciones que realizas y que ayudan a reducir tu huella ecológica.

            .row.mt-3.ex1
                .col-12
                    



            finalize(@evaluate="finalizar" @reset="reset" :resultado="resultado" :right="right" :total="total")
        +scripts
            script.
                //app.$set(app.tempsobj, 'a', [])